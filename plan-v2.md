# Orchestra 线上合奏软件 — 可行性调研与完善计划 (v3)

> 本文件基于 `plan.md` 的原始构想,结合对现有方案、浏览器音频技术、时延对齐策略、记谱/音色/引导库四方面的调研,并纳入已确认的产品决策编写。
> 调研日期: 2026-08-11 | v3 更新: 精准定位入门/初阶用户,砍掉专业定位

---

## 一、执行摘要

**结论: 技术可行,产品空白真实存在,精准定位入门/初阶用户。**

- **定位: 不是「又一个线上排练软件」,而是「第一个让任何人和朋友一起合奏的软件」——音乐版的多人在线游戏,只是它用的是真实乐谱和真实音色。**
- 「所有用户听到相同的乐曲」这一核心需求,**通过「节拍网格对齐 + 时间戳事件」完全可以在浏览器实现**——这是 NINJAM 验证了 20 年的成熟模型,且对网络延迟不敏感。
- 现有 20 余年线上合奏软件历史中,**没有任何产品同时覆盖「入门用户引导演奏 + 进阶 MIDI 输入 + 智能时延对齐 + 曲库生态」**——这是本产品的位置。
- **已砍掉专业用户定位**(不追求实时无损音频互动,那是 JackTrip/Jamulus 的主场);产品 100% 基于音符事件架构,对网络和硬件的要求降到最低。

---

## 二、可行性调研结论

### 2.1 现有方案格局

| 平台                    | 架构                      | 时延处理                          | 输入           | 开源        | 浏览器            | 状态                          |
| ----------------------- | ------------------------- | --------------------------------- | -------------- | ----------- | ----------------- | ----------------------------- |
| **Jamulus**             | 客户端-服务器(服务器混音) | 实时流,30–70ms                    | 音频           | ✅ GPL-2.0  | ❌ 原生           | 活跃                          |
| **JamKazam**            | P2P+服务器混合            | 实时流,专利动态抖动缓冲           | 音频+视频+MIDI | ❌ 商业     | ❌ 原生           | 活跃(订阅制)                  |
| **NINJAM**              | 客户端-服务器(哑中继)     | **节拍网格对齐,延迟=1个interval** | 音频(拒绝MIDI) | ✅ GPL-2.0  | ❌ 原生(有JS移植) | 间歇活跃(2020、2025 均有更新) |
| **JackTrip**            | P2P/中枢                  | 实时流,无损,5–20ms                | 音频           | ✅ MIT core | ⚠️ 有WebRTC路径   | 活跃                          |
| **SonoBus**             | P2P(AoO协议)              | 实时流,每用户抖动缓冲             | 音频           | ✅ GPL-3.0  | ❌ 原生           | 活跃                          |
| **SoundJack**           | P2P                       | 实时流                            | 音频           | ❌ 学术闭源 | ❌ 原生           | 小众                          |
| **BandLab**             | 云DAW                     | **非实时合奏**(录音对齐)          | 音频+MIDI      | ❌ 商业     | ✅                | 活跃                          |
| **jacktrip-webrtc**     | WebRTC DataChannel        | 实时流,~40ms(LAN)                 | 音频           | ✅ GPL-3.0  | ✅                | 2022 停滞                     |
| **Fourhands**           | WebRTC P2P                | 实时,MIDI only,2人                | MIDI           | —           | ✅(Chrome)        | 爱好项目                      |
| **cornelljam/band-app** | WebSocket控制信号         | **服务器端节拍量化**              | 鼠标/触摸      | —           | ✅                | 原型                          |
| **ninjam-js**           | NINJAM协议JS版            | 节拍网格                          | 音频           | ✅          | ✅                | 2016 死亡                     |

**关键学习点:**

- Jamulus 证明了**服务器混音**模式(所有客户端听到完全相同的混音,无需逐客户端同步),但需要服务器靠近所有玩家。
- NINJAM 证明了**「让时延变大而不是变小」**是唯一对任意网络距离都可行的合奏策略——延迟变成一个音乐参数(恰好一个 interval)。
- BandLab 证明浏览器+音乐可行,但**明确不做**实时合奏同步——说明「浏览器+实时合奏」至今仍是空白。
- 浏览器实时音频唯一完整参考是 jacktrip-webrtc(GPL-3.0,已停滞),用 WebRTC **DataChannel 绕过媒体通道**传输 PCM,~40ms(LAN)。

### 2.1a NINJAM 停滞原因调研(2026-08-11,修正早期判断)

**重要修正:** 早期判断「NINJAM 2020 年停滞」不准确。深挖源码历史与社区后,NINJAM 实际是**「间歇性活跃」而非「死亡」**:

- **2020 年 3–11 月有重大更新潮**:服务器 v0.080(多线程运行时)、lobby 模式、MOTD、语音聊天改进、多声道支持。
- **2025 年 5 月 Justin Frankel 仍在改进 ReaNINJAM**(多声道、更高码率),发布 20 周年博客「NINJAM at 20」,并亲自上 ninbot.com 即兴——第一行代码恰在 2005 年 5 月 29 日。
- **生态是活的**:截至 2026-08-11,ninbot.com:2049 有 6 人正在即兴;2026 年 1 月还出现了新客户端 JamWide(GPL-2.0,活跃维护)。

**为什么它「看起来停滞」:**

1. **它是 Cockos 的免费副产品,从无商业模式**——官网自述「part tool, part toy」,公司商业重心在 REAPER。没有收入压力,只有兴趣驱动,于是 2021–2023 出现三年空窗。
2. **独立客户端被 ReaNINJAM 吸收**——仓库 README 明说独立客户端「old and probably need some attention」,REAPER 插件「more fully featured」。官网把独立客户端标为「Very Old, Original, and Obsolete」,推荐客户端就是 REAPER。**协议靠 REAPER 的发布周期活着**,GitHub 主仓库的提交大部分是 WDL 库同步,真正的 NINJAM 改动集中在 2020 和 2025。
3. **一个 interval 的延迟限制音乐风格**(Justin 本人 2007 年承认):「很难演奏编排好的歌曲……每个人的听感是错位的」,适合循环/氛围/节拍类,不适合紧凑即兴——筛掉了大量严肃乐手。
4. **技术形态停在 2005 年**:纯音频、原生应用、拒绝 MIDI、无浏览器(唯一 JS 移植 2016 年死亡)、无移动端。
5. **jammr(类 NINJAM 服务)2025-10 关停**,JamTaba 客户端 2020 年后停更——周边生态确实在收缩,但核心协议和社区仍在。

**对本产品的启示:** 节拍网格**模型**被 NINJAM 验证为网络合奏最稳健的方案,其「停滞」主因是**产品形态**(无商业模式、无引导、无内容、形态过时),而非模型失效。本产品从浏览器、引导式、曲库生态切入,恰好规避了它的全部死因——但也要警惕两点:① 纯循环/节拍网格的「错位感」需要靠引导与曲目设计来化解(这正是 Phase 1 要验证的);② 参考实现选 MIT/BSD 许可的(协议不受版权保护,可自由实现),避免 GPL-3.0 传染。

### 2.2 三种时延范式(核心概念)

| 范式                                 | 代表             | 浏览器可行性                                                   | 本产品态度                |
| ------------------------------------ | ---------------- | -------------------------------------------------------------- | ------------------------- |
| **实时流**(最小化时延)               | Jamulus/JackTrip | ❌ 困难——浏览器本地就 15–40ms,加上网络 70–120ms,远超 25ms 阈值 | **放弃(远期实验)**        |
| **节拍网格对齐**(让时延音乐化)       | NINJAM           | ✅ 稳健——任何网络延迟都能用,延迟=恰好一个 interval             | **✅ 采用(唯一主模式)**   |
| **控制信号/量化**(只传音符,本地合成) | cornelljam       | ✅ 最容易——只有音符事件过网,音频本地合成,无音频硬件要求        | **✅ 采用(叠加在网格上)** |

### 2.3 浏览器技术可行性

**音频渲染(✅ 可行):**

- AudioWorklet 以 128 帧量子运行(~2.9ms@44.1kHz 处理预算),`AudioContext.currentTime` 支持采样级精确调度。
- 本地「按键→出声」总延迟约 **15–40ms**(Chrome macOS 实测 ~11ms 缓冲 + 设备延迟)。
- 参考: Chris Wilson 的 lookahead scheduler 模式是浏览器音序器的标准做法。

**网络传输(✅ 对纯事件架构完全够用):**

- WebSocket 局域网延迟 **<1ms~6ms**,TCP 有序可靠;音符事件极小,是理想的事件通道。
- 纯事件架构**不需要**传输连续音频 → 绕开了 WebRTC 媒体通道的所有问题(NetEQ 拉伸音乐、70–120ms 延迟、SFU 拓扑复杂度)。

**MIDI 输入(⚠️ 有平台缺口,但可接受):**

- Chrome/Edge ✅,Firefox 108+ ⚠️(需装附加组件),**Safari ❌ 完全不支持**(macOS 和 iOS 都是)。
- USB MIDI 2–12ms,蓝牙 MIDI 15–30ms;事件在主线程投递。
- → 产品必须为 Safari 用户提供键盘回退路径(键盘映射本来就是主输入,天然成立)。

**时钟同步(✅ 可行):**

- 浏览器无法跑 NTP(UDP 被禁),但 NTP 四时间戳算法可跑在 WebSocket 上,精度 **±10–30ms**(LAN 更优),需每 ~30s 重同步抵消时钟漂移。

**记谱 / 音色 / 引导库(✅ 全部有成熟选择):**

| 领域       | 推荐                                                                 | 许可证             | 理由                                                                      |
| ---------- | -------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------- |
| 五线谱渲染 | **OSMD**(+底层 VexFlow)                                              | BSD-3-Clause / MIT | MusicXML 原生,总谱直接渲染,分谱用 `Instruments[i].Visible=false` 免费实现 |
| 音色播放   | **smplr**                                                            | MIT                | soundfont-player 的正统后继(原库已归档),CDN 加载无需服务器                |
| 采样库     | **gleitz/midi-js-soundfonts**                                        | MIT                | 按乐器分包(~1–3MB/个),摇滚乐队 5 件乐器约 5–10MB,按需加载                 |
| 引导交互   | **keyfall(MIT)** 作参考 + sightread 的 Wait 模式(GPL-3 只借鉴不抄码) | MIT                | 下落音符+判定窗口(±140/±320ms)+自动演示模式,与需求完全吻合                |
| 鼓组       | GM 标准鼓图(channel 10,音符 35–51)                                   | —                  | 行业标准映射                                                              |

**许可证红线:** SonoBus / jacktrip-webrtc / WebAudioFont 是 GPL-3.0(传染性强),**只学协议、不抄代码**;用 MIT 替代品。

### 2.4 可行性总评

1. **核心需求「所有用户听到相同的乐曲」→ 可行。** 用「服务器节拍时钟 + 时间戳音符事件 + 本地采样级调度」,事件落点误差控制在 ~10–30ms 音乐网格内,与网络延迟基本解耦。这正是你原始构想里「智能方案自动对准时延」的正解。
2. **纯事件架构让产品对网络/硬件的要求降到最低。** 不需要声卡、麦克风、低延迟网络、服务器混音——任何能打开浏览器的设备都能玩,普通互联网可用。
3. **产品定位独特。** 现有工具要么只服务会演奏的人(音频流),要么是纯单机游戏(无合奏);没有产品把「引导式入门演奏」和「进阶 MIDI 输入」放进同一个房间、用同一套节拍网格对齐、并承载完整曲库。

---

## 三、产品定义(已确认决策)

### 3.1 目标用户(精准定位: 入门/初阶)

| 层级        | 用户                               | 输入方式         | 引导                    | 同步模式              |
| ----------- | ---------------------------------- | ---------------- | ----------------------- | --------------------- |
| **L1 入门** | 不懂音乐的人(核心目标)             | 鼠标/电脑键盘    | 下落音符 + 虚拟乐器高亮 | 节拍网格 + 量化事件   |
| **L2 初阶** | 会一点的爱好者、有 MIDI 键盘的玩家 | 键盘 / MIDI 键盘 | 可选高亮 / 谱面跟随     | 节拍网格 + 时间戳事件 |

**明确不服务:** 追求实时无损音频互动的专业乐手(那是 JackTrip/Jamulus 的主场,产品不做)。

### 3.2 已确认的产品决策

1. **同步: 纯节拍网格 + 时间戳事件(唯一主模式)。** 实时音频流(WebRTC)降级为远期实验,不进主线路线图。
2. **MVP 范围: 最小验证版** — 1–2 首内置曲目,2–4 人局域网联机,键盘/鼠标入门模式,验证核心同步与引导体验。
3. **引导交互: 双形态** — 下落音符(音游式)与虚拟乐器高亮两种都支持,用户可切换。
4. **音色: SoundFont 采样** — 真实采样音色,按乐器按需加载控制体积。
5. **MIDI 输入: 保留**,作为初阶用户的升级路径(架构本就是事件流,接入成本极低)。
6. **记谱: 保留完整总谱/分谱(OSMD)**,但重新定位为**教学与差异化功能**——帮助初阶用户学会看谱,而非服务专业演奏。

### 3.3 v1(最小验证版)范围

**包含:**

- 房间系统:创建/加入房间,2–4 人,局域网(WebSocket 直连)
- 输入:键盘按键映射(每件乐器一张映射表),鼠标可选
- 同步:服务器节拍时钟 + 时间戳音符事件 + 节拍网格量化
- 引导:下落音符模式 + 虚拟乐器高亮模式,可切换
- 曲库:1–2 首内置摇滚曲目(如鼓+贝斯+键盘三声部)
- 音色:smplr + SoundFont 采样(鼓/贝斯/键盘)
- 节拍器 + 声部音量混音

**明确不做(v1):**

- ❌ MIDI/音频硬件输入(v2 起)
- ❌ 互联网部署(仅局域网,v2 起)
- ❌ 总谱/分谱显示(v2 起,OSMD)
- ❌ 铜管/交响编制(远期)
- ❌ 实时音频流(远期实验,不在主线)
- ❌ 账号系统、录音回放

### 3.4 成功标准(v1 验收)

- 4 人在局域网内用键盘合奏一首完整曲目,所有人听到的节拍对齐误差 < 30ms
- 完全不懂音乐的人,在 5 分钟内能跟着引导完成自己的声部
- 引导模式无卡顿,判定窗口反馈即时(< 50ms)
- 验证「引导式合奏」是否真的让新手感到乐趣并愿意再来(定性访谈 ≥ 8 人)

---

## 四、技术架构

### 4.1 总体架构

```
┌──────────────────────┐        WebSocket         ┌──────────────────────┐
│  客户端 A (浏览器)    │◄───────信令+音符事件──────►│                      │
│  ┌────────────────┐  │                          │  服务器 (Node.js)     │
│  │ 输入层          │  │                          │  ┌──────────────────┐ │
│  │ 键盘映射/虚拟乐器│  │                          │  │ 房间管理          │ │
│  │ (v2: Web MIDI) │  │                          │  ├──────────────────┤ │
│  ├────────────────┤  │  节拍时钟广播(BPM/BPI)    │  │ 节拍时钟(权威)    │ │
│  │ 同步层          │  │◄─────────────────────────│  ├──────────────────┤ │
│  │ 时钟偏移估计    │  │  事件中继(带服务器时间戳)  │  │ 事件中继          │ │
│  │ 时间戳→音频时钟 │  │◄─────────────────────────│  └──────────────────┘ │
│  ├────────────────┤  │                          │                      │
│  │ 渲染层          │  │   (远期实验:             │                      │
│  │ AudioWorklet    │  │    WebRTC DataChannel)   │                      │
│  │ 调度+音色播放   │  │                          │                      │
│  ├────────────────┤  │                          │                      │
│  │ 引导层          │  │                          │                      │
│  │ 下落音符/乐器高亮│  │                          │                      │
│  │ (v2: OSMD谱面)  │  │                          │                      │
│  └────────────────┘  │                          │                      │
└──────────────────────┘                          └──────────────────────┘
```

**核心原则: 服务器是节拍时钟的权威,但不负责音频混音。** 客户端把音符事件放在共享的节拍网格上,本地合成声音——音频永不因网络丢失或延迟而错位。

### 4.2 同步架构(核心设计,单一主模式)

**节拍网格 + 时间戳事件(所有用户,所有阶段):**

1. 服务器维护 `(beat, tempo, quantum, serverTime)` 会话时钟(Ableton Link 模型)。
2. 客户端加入时做 NTP 式时钟偏移估计(WebSocket ping/pong 四时间戳,取低延迟样本平均,每 30s 重同步)。
3. 客户端发送 `{note, velocity, beatPosition}`(音乐时间,非墙上时间);服务器打时间戳广播。
4. 接收端把事件换算到本地音频时钟(`localAudioTime = serverTime + offset + outputLatency`),用 lookahead scheduler 采样级调度(`start(localAudioTime)`)。
5. 事件量化到量子边界(如每 4 拍),保证所有人听到同一 downbeat。
   → **网络抖动只影响「你何时听说这个事件」,不影响「它落在网格哪里」。**

**远期实验(不阻塞主线):** WebRTC DataChannel PCM 实时音频流(参考 jacktrip-webrtc 机制,只学协议不抄 GPL 代码),用于未来可能的扩展方向,不作为产品承诺。

**为什么不用服务器混音(Jamulus 模式):** v1 是键盘/音符事件输入,本地合成天然可行且省带宽;服务器混音是实时音频流时代的优化,不在本产品方向内。

### 4.3 技术选型

| 组件          | 选型                                     | 说明                                |
| ------------- | ---------------------------------------- | ----------------------------------- |
| 前端框架      | React + TypeScript + Vite                | 引导 UI 复杂,React 生态成熟         |
| 音频引擎      | Web Audio API + AudioWorklet             | 采样级调度,lookahead scheduler 模式 |
| 网络          | WebSocket(ws 库)                         | 信令 + 音符事件 + 节拍时钟          |
| 音色          | smplr + gleitz/midi-js-soundfonts        | 均为 MIT;按乐器按需加载             |
| 记谱(v2)      | OSMD + VexFlow                           | MusicXML 总谱 + 分谱隐藏            |
| MIDI 输入(v2) | Web MIDI API                             | Chrome/Edge 优先,Safari 键盘回退    |
| 服务器        | Node.js + ws                             | v1 单进程即可                       |
| 测试          | Vitest(单元) + Playwright(E2E)           | 同步逻辑重点测试                    |
| 曲目格式      | 自研 JSON(音符+时间戳+声部) 或 简化 MIDI | v1 先用自研格式,后迁移              |

### 4.4 关键模块

**客户端:**

- `InputLayer`: 键盘映射表(每乐器),按键→音符事件,防连发;(v2) Web MIDI 输入
- `SyncLayer`: 时钟偏移估计、节拍网格、事件时间戳转换、量化
- `RenderLayer`: AudioWorklet 调度器、smplr 音色播放、混音、节拍器
- `GuideLayer`: 下落音符渲染 + 判定系统;虚拟乐器高亮渲染(两模式共用事件流);(v2) OSMD 谱面跟随高亮
- `RoomLayer`: 房间连接、加入/离开、曲目与声部选择

**服务器:**

- `RoomManager`: 房间创建/加入/状态同步
- `BeatClock`: 权威节拍时钟(BPM/BPI 可配置),广播状态
- `EventRelay`: 接收音符事件 → 打服务器时间戳 → 广播给房间内其他人

---

## 五、分阶段路线图

### Phase 0 — 技术验证原型(约 1–2 周,先于一切)

**目标: 消除 3 个最大技术风险。**

| 验证项                   | 方法                           | 通过标准                   |
| ------------------------ | ------------------------------ | -------------------------- |
| ① 键盘→音色延迟          | 本地 AudioWorklet + smplr 演奏 | 按键到出声 < 50ms          |
| ② WebSocket 时钟同步精度 | 双客户端对服务器做偏移估计     | LAN 内偏移估计误差 < 10ms  |
| ③ 节拍网格事件对齐       | 双客户端键盘敲同一节奏         | 对齐误差 < 30ms,无累积漂移 |

**产出:** 可运行的「两个浏览器敲键盘合奏节拍器」demo。若 ③ 通过,核心架构成立,进入 Phase 1。

### Phase 1 — MVP 最小验证版(约 4–6 周)

- 房间系统(局域网)、键盘输入、双引导模式、SoundFont 音色、节拍网格同步、1–2 首内置曲目
- **验收:** 4 人局域网合奏完整曲目,对齐 < 30ms;新手 5 分钟上手
- 里程碑: 内部小范围邀请 8–12 人试用,收集「引导模式是否真的让新手玩起来」的定性反馈

### Phase 2 — 输入升级 + 记谱 + 互联网(约 4–6 周)

- Web MIDI 输入(Chrome/Edge 优先,Safari 回退键盘)
- 记谱显示:OSMD 渲染曲目的总谱/分谱,可跟随高亮(教学向)
- 曲库扩充至 5–10 首,加入曲目编辑器(录制→谱面→分享)
- 互联网部署:服务器上云 + HTTPS(WSS)+ 房间码邀请
- **验收:** MIDI 用户 + 键盘用户同房间合奏;跨城网络下事件对齐仍 < 50ms;初阶用户能借谱面跟随学会看谱

### Phase 3 — 内容与教学生态(约 6–8 周)

- 曲库平台化:曲目上传/分享/评分,建立内容护城河
- 教学引导内容:分步教程、乐器入门课(键盘/鼓/贝斯)
- 录音回放:事件流录制,可回放自己的演奏并对照
- 更多编制模板:铜管乐队、小型管弦(基于同一声部模板机制)
- **验收:** 用户能自产曲目并邀请朋友合奏;新手从 0 到完成一首曲目的路径完整

### 远期实验(不承诺上线时间)

- 实时音频流:WebRTC DataChannel PCM(参考 jacktrip-webrtc 机制)
- 触屏移动端支持
- 仅在主线稳定后评估

---

## 六、风险清单与对策

| #   | 风险                                                                  | 等级 | 对策                                                                                  |
| --- | --------------------------------------------------------------------- | ---- | ------------------------------------------------------------------------------------- |
| 1   | **Safari 无 Web MIDI**                                                | 中   | 键盘回退路径(v1 本就以键盘为主);iOS 用户以触摸/键盘为主                               |
| 2   | **「引导合奏」可能被玩成纯游戏**——新手只跟着下落音符按,不产生音乐学习 | 中   | 双引导模式中乐器高亮保留真实乐器映射;v2 谱面跟随衔接教学;Phase 3 教程内容强化学习路径 |
| 3   | **曲库版权与内容供给**                                                | 高   | 只收录公有领域/原创/CC 授权曲目;Phase 3 曲目编辑器让用户自产,形成 UGC 护城河          |
| 4   | **GPL-3.0 传染**(sonobus/jacktrip-webrtc/webaudiofont)                | 中   | 只学协议不抄代码;音色用 MIT 的 smplr 而非 GPL 的 webaudiofont                         |
| 5   | **时钟漂移累积**                                                      | 低   | 每 30s 重同步;节拍网格天然容忍小漂移(事件量化)                                        |
| 6   | **双引导模式开发量**                                                  | 中   | 两模式共用事件流与判定核心;MVP 先落地下落音符,乐器高亮紧随其后(两者都在 v1)           |
| 7   | **浏览器本地音频延迟(15–40ms)**                                       | 低   | Phase 0 先实测;用 `getOutputTimestamp()` 校准;向用户显示延迟读数                      |
| 8   | **主线程阻塞影响事件时序**                                            | 低   | 渲染/调度在 AudioWorklet 线程;主线程保持轻量;事件用 `performance.now()` 打时间戳      |
| 9   | **总谱/分谱保留增加 Phase 2 复杂度**                                  | 低   | OSMD 渲染成本低(BSD 许可、MusicXML 原生);只做「渲染+跟随高亮」,不做编辑               |

---

## 七、开放问题与建议默认值

以下问题不阻塞规划,已给出建议默认值,如无异议按默认执行:

1. **前端框架:** 建议 React + TypeScript + Vite(引导 UI 复杂)。如偏好轻量可用 vanilla TS。→ 默认 **React + TS**。
2. **曲目数据格式:** 建议自研 JSON 事件格式(v1),预留 MIDI 导入能力(Phase 2)。→ 默认 **自研 JSON**。
3. **MVP 曲目选择:** 建议「鼓 + 贝斯 + 键盘」三声部,节奏型简单、重复度高,适合循环式节拍网格。→ 默认 **三声部**。
4. **是否要移动端:** v1 仅桌面浏览器(v1 是键盘输入,天然桌面);触屏支持留待远期实验。→ 默认 **桌面优先**。
5. **商业化方向(远期):** 曲库订阅、乐队房间增值服务(如历史合奏回放)、UGC 曲目市场分成。→ 默认 **暂不设计,Phase 3 再定**。

---

## 八、下一步

1. ✅ 可行性调研完成(本文件第二节)
2. ✅ 产品决策确认(第三节,含精准定位调整)
3. ⏳ 确认第七节开放问题的默认值
4. → 进入 Phase 0 技术验证原型(需单独的实施计划)
